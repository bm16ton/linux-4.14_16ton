--- imlibrenderer/fbrenderer/fbrenderer.c	(revision 30)
+++ imlibrenderer/fbrenderer/fbrenderer.c	(working copy)
@@ -470,6 +470,7 @@
 			default : tell(0, "fbdevout.c: color depth not supported "
                         "-> %i bits per pixel", fb_vinfo.bits_per_pixel);
 		}
+		system(cString::sprintf("echo '' > %s", fb_dev_name));
 
 	}

