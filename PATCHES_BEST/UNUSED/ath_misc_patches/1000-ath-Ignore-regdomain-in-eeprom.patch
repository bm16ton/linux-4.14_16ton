diff -aur orig/drivers/net/wireless/ath/regd.c new/drivers/net/wireless/ath/regd.c
--- orig/drivers/net/wireless/ath/regd.c	2014-12-14 03:36:23.576666664 -0500
+++ new/drivers/net/wireless/ath/regd.c	2014-12-14 03:43:57.473333326 -0500
@@ -341,6 +341,8 @@
 	struct ieee80211_channel *ch;
 	unsigned int i;
 
+	return;
+
 	for (band = 0; band < IEEE80211_NUM_BANDS; band++) {
 		if (!wiphy->bands[band])
 			continue;
@@ -374,6 +376,8 @@
 {
 	struct ieee80211_supported_band *sband;
 
+	return;
+
 	sband = wiphy->bands[IEEE80211_BAND_2GHZ];
 	if (!sband)
 		return;
@@ -402,6 +406,8 @@
 	struct ieee80211_channel *ch;
 	unsigned int i;
 
+	return;
+
 	if (!wiphy->bands[IEEE80211_BAND_5GHZ])
 		return;
 
@@ -632,6 +638,9 @@
 	const struct ieee80211_regdomain *regd;
 
 	wiphy->reg_notifier = reg_notifier;
+
+	return 0;
+
 	wiphy->regulatory_flags |= REGULATORY_STRICT_REG |
 				   REGULATORY_CUSTOM_REG;
 
diff -aur orig/net/wireless/reg.c new/net/wireless/reg.c
--- orig/net/wireless/reg.c	2014-12-14 03:36:29.286666666 -0500
+++ new/net/wireless/reg.c	2014-12-14 03:40:59.469999986 -0500
@@ -2072,6 +2072,8 @@
 	enum environment_cap env = ENVIRON_ANY;
 	struct regulatory_request *request = NULL, *lr;
 
+	return;
+
 	/* IE len must be evenly divisible by 2 */
 	if (country_ie_len & 0x01)
 		return;
@@ -2268,6 +2270,7 @@
 
 void regulatory_hint_disconnect(void)
 {
+	return;
 	REG_DBG_PRINT("All devices are disconnected, going to restore regulatory settings\n");
 	restore_regulatory_settings(false);
 }
